---
- name: Bootstrap the outer hypervisor host
  import_playbook: bootstrap/site.yml
  tags:
    - bootstrap-host
- name: Build or refresh the bastion support VM
  import_playbook: bootstrap/bastion.yml
  tags:
    - bastion
    - support-vms
- name: Stage the project onto the bastion
  import_playbook: bootstrap/bastion-stage.yml
  tags:
    - bastion-stage
