apiVersion: operators.coreos.com/v1alpha1
kind: Subscription
metadata:
  name: {{ openshift_keycloak.subscription_name }}
  namespace: {{ openshift_keycloak.namespace }}
spec:
  channel: {{ openshift_keycloak.channel }}
  installPlanApproval: {{ openshift_keycloak.installplan_approval }}
  name: rhbk-operator
  source: {{ openshift_keycloak.source }}
  sourceNamespace: {{ openshift_keycloak.source_namespace }}
